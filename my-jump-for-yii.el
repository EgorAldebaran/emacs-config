(defun source-jump-config()
  "место настойки всех прыжков"
  (interactive)
  (bookmark-jump "my-jump-for-yii.el"))

(defun go-bookmark-hokma()
	"попытка прыгнуть в хокму людей"
	(interactive)
	(bookmark-jump "hokma-human"))

(defun jump-bina-index()
    "прыгаю в бина индекс"
  (interactive)
  (bookmark-jump "bina-index"))

(defun jump-feedback()
  "go to feedback"
  (interactive)
  (bookmark-jump "bina-one-feedback")) 

(defun jump-tiferet-human()
    "прыгаю в бина индекс"
  (interactive)
  (bookmark-jump "tiferet-human"))

(defun jump-to-home()
  "прыгаю домой в родной лисп"
  (interactive)
  (bookmark-jump "aaegor"))


(defun jump-keter()
	"попытка прыгнуть в keter людей"
	(interactive)
	(bookmark-jump "source.sql"))


(defun jump-layout()
  "прыгаем в шаблон"
  (interactive)
  (bookmark-jump "best.php"))

(defun jump-test()
  "прыгаем в тестовую сфиру для уроков"
  (interactive)
  (bookmark-jump "test.php"))
